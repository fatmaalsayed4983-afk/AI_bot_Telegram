version: '3.8'

services:
  bot:
    build: .
    container_name: ai_telegram_assistant
    restart: always
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./temp:/app/temp
    env_file:
      - .env
